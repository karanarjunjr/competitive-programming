int power(int a, int b, int M) {
    int ans = 1;
    while (b > 0) {
        if ((b & 1))
            ans = (a * ans) % M;
        a = (a * a) % M;
        b = (b >> 1);
    }
    return ans;
}

int modInverse(int a, int M) {
    return power(a, M - 2, M);
}
